// jest.setup.ts

//
beforeAll(async () => {
  // postgresTypeormConnection = await appTypeormManager.connect({
  //   reuseConnexion: true,
  // });
});
afterAll(async () => {
  // postgresTypeormConnection.close();
});
