export const environment = {
  apiUrl: "http://localhost:4200/", // "proxy.conf.json" configuration will be used to proxy API requests to backend bypassing cross-origin requests security on firefox
  production: false,
};
